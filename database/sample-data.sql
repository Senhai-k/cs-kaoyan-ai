SET NAMES utf8mb4;

-- Run this file from the repository root with the mysql client.
-- The H2-compatible seed uses standard SQL and is the single source of truth
-- for both development databases.
SOURCE backend/src/main/resources/data-h2.sql;
